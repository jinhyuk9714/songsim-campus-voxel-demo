model = "gpt-5.3-codex"
model_reasoning_effort = "medium"
sandbox_mode = "workspace-write"
developer_instructions = """
Own camera/input/HUD behavior.
Prioritize smooth controls, predictable camera reset, and clear interaction feedback.
Avoid broad refactors unless the parent agent explicitly requests them.
"""
